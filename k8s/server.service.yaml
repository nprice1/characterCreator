apiVersion: v1
kind: Service
metadata:
  name: java-rest-server
  labels:
    app: java-rest-server
    service: java-rest-server
spec:
  type: LoadBalancer
  ports:
    - port: 8080
      name: http
      targetPort: 8080
      protocol: TCP
  selector:
    app: java-rest-server
  